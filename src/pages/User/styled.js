import styled from 'styled-components';

export const Main = styled.main`
  p {
    margin: 0;
  }

  .card {
    /* max-width: 500px; */
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .sticky {
    position: sticky;
    top: 80px;
  }
`;
